# ShopTracker environment configuration
# Fill in the correct password and adjust host if needed.

# Postgres connection URL (used by server and drizzle)
# Include search_path so ShopTracker objects resolve under the `shoptracker` schema first.
DATABASE_URL=postgresql://shopsuitedbuser:WeMakePigFloors@10.4.2.49:5432/shop_suite_production?options=-c%20search_path%3Dshoptracker%2Cpublic

# Node environment
NODE_ENV=development

# Optional: host binding for dev LAN access (0.0.0.0 to listen on all interfaces)
HOST=10.4.2.26
